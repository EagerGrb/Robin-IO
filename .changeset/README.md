# Changesets

Use Changesets to record package-level release notes before publishing.

```bash
npm run changeset
npm run changeset:status
npm run changeset:version
```

The example apps and private root workspace are ignored. Public packages under `packages/*` are eligible for release notes and version bumps.
